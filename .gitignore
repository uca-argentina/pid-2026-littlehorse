# ---------- Claude Code ----------
.claude/settings.local.json
.claude/*.local.json

# ---------- .NET ----------
[Bb]in/
[Oo]bj/
[Dd]ebug/
[Rr]elease/
[Tt]est[Rr]esults/
*.user
*.suo
*.userosscache
*.sln.docstates
project.lock.json
artifacts/
coverage/
*.coverage
*.cobertura.xml
BenchmarkDotNet.Artifacts/

# ---------- Node / Angular ----------
node_modules/
dist/
.angular/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.tsbuildinfo

# ---------- Playwright ----------
playwright-report/
test-results/
blob-report/
playwright/.cache/

# ---------- Secrets: NEVER commit these ----------
# Web Push VAPID keys, payment gateway credentials, connection strings.
.env
.env.*
!.env.example
appsettings.Development.json
appsettings.*.Local.json
*.pfx
*.p12
*.key
secrets.json
.mcp.local.json

# ---------- IDE / OS ----------
.vs/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# .vscode is versioned on purpose (shared team settings and extensions),
# but each person's own overrides are not.
.vscode/*.local.json

# Snapshot of the API contract, fetched by "pnpm run generate:api". Not a second
# source of truth: the API is, and the types it generates are committed.
frontend/openapi.json
