/**
 * Shown where a product's picture would go when it has none, or when the one
 * it has cannot be fetched. Served with the app, so it is there even when the
 * storage is not.
 */
export const PRODUCT_PLACEHOLDER = '/images/product-placeholder.svg';
